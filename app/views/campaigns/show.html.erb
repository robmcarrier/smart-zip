<%= @campaign.name %>
<br>
<%= @campaign.camp_type %>
<ul>
  <% @campaign.respondents.each do |r| %>
  <li>Name:<%= r.name %></li>
  <li>Phone:<%= r.phone_number %></li>
  <hr>
  <% end %>
</ul>
<%= link_to "home", root_path %>
